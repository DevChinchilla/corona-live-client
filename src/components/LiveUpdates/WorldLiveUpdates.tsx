import React from "react";
import styled from "styled-components";

const Wrapper = styled.div``;

type Props = {};

const WorldLive: React.FC<Props> = ({}) => {
  return <Wrapper>WorldLive</Wrapper>;
};

export default WorldLive;
