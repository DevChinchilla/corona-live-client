import React from "react";
import styled from "styled-components";

const Wrapper = styled.div``;

type Props = {};

const DomesticLive: React.FC<Props> = ({}) => {
  return <Wrapper>DomesticLive</Wrapper>;
};

export default DomesticLive;
